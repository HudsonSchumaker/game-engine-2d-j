package com.schumakerteam.alpha.sandbox;

public class SetValueTest {

    public static void main(String ...args) {
        var line = "12,22,33,44,55";
        var split = line.split(",");

        for (var pos : split) {
            var posX = pos.substring(0, 1);
            var posY = pos.substring(1, 2);

            var x = 0;
        }
    }
}
